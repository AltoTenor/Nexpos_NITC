int main()
{
decl
    int a,b,temp;
    str inp,ans;
enddecl
begin
    inp = "Enter Input: ";
    ans = "GCD is ";
    temp = exposcall ( "Write" , -2, inp );
    temp = exposcall ( "Read" , -1, a );
    temp = exposcall ( "Read" , -1, b );
    while(b!=0)do
        temp = b;
        b = (a % b);
        a = temp;
    endwhile;
    temp = exposcall ( "Write" , -2, ans );
    temp = exposcall ( "Write" , -2, a );
    return 0;
end
}